{
  "buildCommand": "pnpm --filter @workspace/orange-logo run build",
  "outputDirectory": "artifacts/orange-logo/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
